<h2><%= @user.name %>のユーザー登録情報</h2>

<div class="row mt-3 p-3 border rounded">
  <div class="col-md-3">
    <p>ID</p>

    <p>ニックネーム</p>

    <p>メールアドレス</p>

    <p>ステータス</p>
  </div>

  <div class="col-md-9">
    <p><%= @user.id %></p>

    <p><%= @user.name %></p>

    <p><%= @user.email %></p>

    <p><%= @user.is_active ? '有効' : '退会済み' %></p>

    <%= link_to '編集する', edit_admin_user_path(@user), class: "btn btn-success" %>
  </div>
</div>